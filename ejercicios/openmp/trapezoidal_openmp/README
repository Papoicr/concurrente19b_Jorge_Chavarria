La función elegida es 3ay² = x(x-a)² con a = 3

El trapezoidal pthreads funciona con mapeo ciclico pero tambien tiene unos métodos para usar mapeo por bloques, por eso hay unos métodos que no se usan, esto es por que originalmente había usado bloques pero era muy ineficiente, así que lo cambié cuando hice las pruebas
